-- TODO: Should we name these Matchers instead?

---@class terminal-diagnostics.parser.Parser
local Parser = {}

function Parser.new()
end

function Parser.parse(buffer, lnum, col)
end

function Parser.can_parse(buffer, lnum, col)
end

return Parser
